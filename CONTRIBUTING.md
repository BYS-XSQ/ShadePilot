# Contributing

提交较大功能前请先通过 Issue 说明问题与预期结果。ShadePilot 以明确、轻量和可恢复为产品边界，不接受与显示调节目标无关的功能膨胀。

## 要求

- 不得读取游戏进程、注入进程、模拟输入或添加遥测。
- 所有硬件写入必须具备错误处理，并保留清晰的恢复路径。
- 新依赖和素材必须说明来源与许可证。
- 不要提交本地设置、构建产物、日志或凭据。

## 构建检查

```powershell
.\build.ps1
```

Pull Request 请说明测试的 Windows 版本、显示器、连接方式、DDC/CI 状态和恢复测试结果。
